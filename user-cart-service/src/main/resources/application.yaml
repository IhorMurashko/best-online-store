server:
  port: 8080
spring:
  application:
    name: cart-service
  cache:
    type: caffeine
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always