server:
  port: 8080

spring:
  application:
    name: test
logging:
  level:
    web: debug
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always